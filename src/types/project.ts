export type Project = {
    logo: string,
    title: string,
    description: string,
    techStack: string[],
    link?: string,
    source: string
};

export interface I_Heading {
    title: string
}